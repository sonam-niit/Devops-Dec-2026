# String Manupulation

1. String manupulation 
2. String format with prefix and suffix

*Execute Code as mentioned in PPT*

3. Iterate Arguments usinf while Loop **as shown in ppt**
4. Execute Special Argument demo as well by passing 4 arguments

5. Run the code with GetOps Option
    - which is actually creating a command by passing some flags
    - try to write the code and run it with diffrent flags
    - script.sh -f file.txt
    - script.sh -o output.txt
    - script.sh -f file.txt -o output.txt

    - see the diffrences in the output