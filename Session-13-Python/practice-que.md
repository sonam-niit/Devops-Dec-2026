# Practice Task

1. Create Class called DO and some attributes like name and breed.
    - add property is_sleeping
    - create init method (constructor)
    - add some function
    - create objects and understand the usage

*Hint: check ppt for demo code*

2. Create Class Car
    - with attributes: brand, model and  mialeage
    - methods:
        + start()
        + stop()
        + display_info()
    - add constructor (init) which inialize all attributes
    - use it by creating Objects

3. Shopping Cart Example
    - create class Shopping Cart
    - which allows adding and removing items using List
    - methods
        + addItem(self,item)
        + removeItem(self,item)
        + view_cart() # iterate list and show all items
        + calculate_total_amount

4. Account Class
    - attributes:
        + acc_no
        + balance
        + account_holder_name
    - methods:
        + deposit()
        + withdraw()
        + display_details()
        + chec_balance()
    - create objects for usage

5. Employee Salary Calculator
    - class named Employee
    - attributes:
        + name
        + salary
        + bonus
    - functions:
        + total_salary: returns salary + bonus
    - create objects and check