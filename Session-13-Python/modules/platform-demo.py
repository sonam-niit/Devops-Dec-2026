import platform as pt # use pt as alias

print(pt.release()) # OS Release Version
print(pt.system()) # OS Name
print(pt.version()) # detailed OS Version
print(pt.machine()) #machine Type
print(pt.node()) #Computer Network Host name
print(pt.processor())
print(pt.python_version())
print(pt.python_build())
print(pt.python_compiler())