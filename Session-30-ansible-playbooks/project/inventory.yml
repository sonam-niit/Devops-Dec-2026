all:
  hosts:
    server1:
      ansible_host: ec2-32-192-180-157.compute-1.amazonaws.com 
      ansible_user: ec2-user
      ansible_ssh_private_key_file: ~/.ssh/sonamkey.pem

# verify inventory is OK or not
# ansible-inventory -i inventory.yml --list 